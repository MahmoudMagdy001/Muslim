import 'package:flutter/material.dart';

class QiblahLoadingWidget extends StatelessWidget {
  const QiblahLoadingWidget({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: CircularProgressIndicator());
}
